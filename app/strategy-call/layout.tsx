export const metadata = {
  title: "Strategy Call",
  description:
    "Request a systems strategy consultation and get a structured plan for automation, CRM, and growth.",
};

export default function StrategyCallLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
